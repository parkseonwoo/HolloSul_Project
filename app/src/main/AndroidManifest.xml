<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hollosul">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LogoActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"/>
        <activity android:name=".Category"/>
        <activity android:name=".Food"/>
        <activity android:name=".FoodDetail"/>
        <activity android:name=".Login1"/>
        <activity android:name=".Friend"/>
        <activity android:name=".Recipe"/>
        <activity android:name=".Near_Friend"/>
        <activity android:name=".LMainActivity"/>
        <activity android:name=".LLoginActivity"/>
        <activity android:name=".FindActivity"/>
        <activity android:name=".ProfileActivity"/>
    </application>

</manifest>